# Optional dev / CI — does not affect Phase 1 runtime
-r requirements.txt
-r requirements-backend.txt
pytest>=8.0
pytest-rerunfailures>=14.0  # auto-retry a rare cross-test ORM-aliasing flake (see issue #8)
httpx>=0.28.1
moto[s3]>=5.2.2
pyarrow>=15.0.0  # Parquet fixtures for the input-adapter tests (agentic/adapters)

# Lint / type-check gate (see ruff.toml, mypy.ini, .github/workflows/ci.yml).
# Ruff is upper-bounded so new rule releases can't turn CI red without a deliberate bump.
ruff>=0.13.3,<0.14
mypy>=1.11
# Coverage measurement + CI floor (see .coveragerc, .github/workflows/ci.yml).
pytest-cov>=5.0.0
# Supply-chain audit (see .github/workflows/security.yml).
pip-audit>=2.7.3
