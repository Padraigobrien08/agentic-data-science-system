{
  "suite_id": "suite_smoke",
  "output_dir": "data/evaluation",
  "cases": [
    {
      "case_id": "smoke_aapl_msft",
      "description": "Minimal deterministic smoke benchmark for core outputs.",
      "tags": ["smoke", "pipeline", "deterministic"],
      "input": {
        "mode": "live",
        "tickers": ["AAPL", "MSFT"],
        "goal": "Run anomaly analysis for a core peer set and produce baseline artifacts.",
        "refresh": false,
        "policy": {
          "requires_explicit_live_opt_in": true,
          "fair_access_policy": "sec_fair_access_operator_invoked",
          "allow_merge_blocking": false,
          "normal_user_visible": false,
          "freshness_window_seconds": 300
        }
      },
      "max_runtime_seconds": 120.0,
      "expected_artifacts": {
        "expected_statuses": ["passed", "skipped"],
        "required_keys": ["panel_csv", "features_csv", "anomalies_csv", "report_md"],
        "items": [
          {"key": "panel_csv", "required": true},
          {"key": "features_csv", "required": true},
          {"key": "anomalies_csv", "required": true},
          {"key": "report_md", "required": true, "must_contain": ["# EDGAR", "## Findings"]},
          {"key": "manual_validation_csv", "required": false},
          {"key": "metric_coverage_summary_csv", "required": false}
        ],
        "qualitative_checks": [
          "Trustworthiness-related artifacts are present or explicitly marked unavailable."
        ]
      },
      "expected_findings": {
        "required_types": ["anomaly"],
        "required_categories": [],
        "total_count": {"minimum": 1},
        "by_type_min_counts": {"anomaly": 1},
        "qualitative_expectations": [
          "Unified findings include at least one materially unusual signal."
        ]
      },
      "expected_metrics": {
        "runtime_seconds": {"minimum": 0, "maximum": 120}
      },
      "qualitative_expectations": [
        "Output report remains readable and includes a findings section."
      ],
      "notes": "Policy scaffold for operator-invoked live validation; execution logic remains wired in runner TODOs and this suite is not the default regression path."
    }
  ]
}
