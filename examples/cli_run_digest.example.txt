EXAMPLE: typical stdout from `python3 -m edgar_project.cli run ...` or `demo` (non-`--json`).
Static copy for reviewers — not captured from a live SEC run.

────────────────────────────────────────────────────
 Run summary
────────────────────────────────────────────────────
  status:     success
  tickers:    MSFT AAPL NVDA
  run_id:     00000000-0000-0000-0000-00000000EXAM
  plan:       anomaly analysis
  steps:      resolve → fetch → panel → features → anomalies → report
  summary:    Orchestration completed.

 Where outputs go
   data/raw/            — SEC cache (live fetch)
   data/processed/      — panel & features
   data/artifacts/      — anomalies, combined findings, report
   data/evaluation/     — benchmarks only (not this pipeline)
   More: <repo>/data/README.md

 Key files (this run)
   Report (Markdown)              <repo>/data/artifacts/report.md
   Unified findings               <repo>/data/artifacts/unified_findings.csv
   Anomalies                      <repo>/data/artifacts/anomalies.csv
   …

 Counts
   combined findings:     14
   raw anomalies:         28
   companies:             3
   metrics (distinct):    8

 Top 5 combined findings (severity order)
   1. MSFT (CIK 789019)  net_margin  [peer_relative]  high  score=2.1
      Net margin vs peers in top decile
   2. …

   14 combined finding rows across 3 companies and 8 distinct metrics.
────────────────────────────────────────────────────
