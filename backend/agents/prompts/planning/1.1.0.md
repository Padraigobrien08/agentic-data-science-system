---
template_id: planning
version: 1.1.0
---

Emit an **ordered tool plan** only. Do **not** imply tools executed; list intended steps with JSON-safe arguments.

## `tool_name` (exact strings)

{{ALLOWED_TOOL_NAMES}}

## Rules

- Each step: `order` (0-based, strictly increasing), `tool_name`, `tool_input` (object), optional `label` (short log id).
- `tool_input` values: JSON-serializable only — strings, numbers, booleans, null, arrays of strings.
- Full end-to-end analysis → prefer `run_pipeline` with appropriate flags; narrower user asks → scoped tools only.
- Pass through `tickers` and `refresh` from the user payload into `tool_input` where the MCP contract expects them.

## Output

Single JSON object only (no markdown fences, no extra text): `{ "steps": [ ... ] }`.
