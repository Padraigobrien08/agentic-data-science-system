"""
MCP surface for the investigation platform.

Exposes the *platform* (commissioning investigations, reading hypotheses, evidence, and
artifacts) as MCP tools and resources, as a client of the ``/v1`` API. The deterministic
EDGAR computation tools live separately in ``edgar_project.mcp``.
"""
