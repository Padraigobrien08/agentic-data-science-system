# Phase 22 Validation

## Commands

- `cd frontend && npm run test -- src/components/chat-shell/chat-composer.test.tsx src/components/chat-shell/chat-shell.test.tsx`
- `cd frontend && npm run build`

## Must Hold True

- The left rail reads like chat, not workspace navigation.
- A new chat can be started from the primary shell without sending the user through workspace terminology.
- The projects index and landing entry copy no longer frame the product primarily as a workspace manager.
