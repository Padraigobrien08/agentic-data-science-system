---
phase: 01-run-isolation
plan: 04
type: execute
wave: 4
depends_on:
  - 01-02
  - 01-03
files_modified:
  - tests/test_run_isolation_workspace.py
  - tests/test_run_isolation_overlap.py
  - tests/mcp/conftest.py
  - tests/mcp/test_tools.py
  - tests/test_mcp_orchestration_artifact_contract.py
  - tests/orchestration/test_phase3_orchestration.py
  - tests/test_report_credibility.py
  - tests/test_trustworthiness_contracts.py
autonomous: true
requirements:
  - EXEC-01
  - EXEC-02
must_haves:
  truths:
    - "Overlapping runs keep distinct processed and artifact paths per EXEC-01 and D-02."
    - "MCP and orchestration regression suites preserve workspace-scoped artifact references and the `generate_report:explicit_paths` contract per EXEC-02 and D-04."
    - "Report credibility and trustworthiness regressions treat workspace-scoped paths as the normal branch and shared defaults as explicit legacy opt-in only per D-03 and D-04."
  artifacts:
    - path: "tests/test_run_isolation_overlap.py"
      provides: "Concurrent-run collision regression coverage"
      contains: "test_overlapping_runs_keep_distinct_artifact_paths"
    - path: "tests/mcp/test_tools.py"
      provides: "MCP/report explicit-path regression coverage"
      contains: "run_workspace"
  key_links:
    - from: "tests/test_run_isolation_overlap.py"
      to: "src/pipeline_runner.py"
      via: "two workspaces with different run_scoped_id values"
      pattern: "run-a"
    - from: "tests/orchestration/test_phase3_orchestration.py"
      to: "edgar_project/orchestration/planner.py"
      via: "generate_report explicit-path step label"
      pattern: "generate_report:explicit_paths"
---

<objective>
Add focused regression coverage that proves run isolation, provenance, and no-cwd execution are real behaviors rather than implied contracts.

Purpose: Lock in Phase 1 so concurrent runs, report generation, backend execution, and non-DB entry points cannot drift back to shared-path behavior without test failures.
Output: New isolation-focused tests plus updates to existing MCP/orchestration/report regression suites.
</objective>

<execution_context>
@/Users/padraigobrien/.codex/get-shit-done/workflows/execute-plan.md
@/Users/padraigobrien/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-run-isolation/01-CONTEXT.md
@.planning/phases/01-run-isolation/01-RESEARCH.md
@tests/mcp/conftest.py
@tests/mcp/test_tools.py
@tests/test_mcp_orchestration_artifact_contract.py
@tests/orchestration/test_phase3_orchestration.py
@tests/test_report_credibility.py
@tests/test_trustworthiness_contracts.py
@tests/test_backend_foundation.py
@backend/services/edgar_pipeline_execution_service.py
@src/pipeline_runner.py
@main.py
@edgar_project/cli.py

<interfaces>
From `tests/mcp/conftest.py`:
```python
def tmp_artifact_paths(tmp_path: Path) -> dict[str, Path]:
    ...
```

From `tests/test_mcp_orchestration_artifact_contract.py`:
```python
_merge_artifact_paths(accum, env)
```

From `tests/orchestration/test_phase3_orchestration.py`:
```python
assert ARTIFACT_KEY_PANEL in out.artifact_paths
assert ARTIFACT_KEY_REPORT in out.artifact_paths
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add focused regression tests for workspace construction and overlap isolation</name>
  <files>tests/test_run_isolation_workspace.py, tests/test_run_isolation_overlap.py</files>
  <read_first>
tests/test_run_isolation_workspace.py
tests/test_run_isolation_overlap.py (new file; confirm it does not exist yet)
src/pipeline_runner.py
edgar_project/run_workspace.py
tests/test_run_isolation_execution_service.py
  </read_first>
  <behavior>
    - Test 1: `test_workspace_paths_are_persisted_and_run_scoped` keeps the workspace payload and explicit-input assertions aligned with the backend provenance behavior already added in Plan 03.
    - Test 2: `test_overlapping_runs_keep_distinct_artifact_paths` creates two workspaces such as `run-a` and `run-b`, writes Phase 1 artifacts for both, and asserts every shared role key points to different filesystem paths.
  </behavior>
  <action>Add the remaining focused regression modules per EXEC-01 and EXEC-02 after the runtime behavior exists. Expand `tests/test_run_isolation_workspace.py` to keep `RunWorkspacePayload` serialization, legacy-mode opt-in behavior, and the rule that `manual_validation_csv` remains an explicit shared input path rather than a copied workspace artifact aligned with the backend provenance path now exercised in Plan 03. Create `tests/test_run_isolation_overlap.py` with a fixture that builds two `RunWorkspace` instances under the same root and asserts `panel`, `features`, `anomalies`, `report`, and `unified_findings` do not collide between runs.</action>
  <acceptance_criteria>
    - `tests/test_run_isolation_overlap.py` contains `test_overlapping_runs_keep_distinct_artifact_paths`.
    - `tests/test_run_isolation_workspace.py` contains `test_workspace_paths_are_persisted_and_run_scoped` and `test_manual_validation_csv_remains_explicit_input`.
    - The new overlap test asserts at least `panel`, `features`, `anomalies`, and `report` paths are different across two runs.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/test_run_isolation_workspace.py tests/test_run_isolation_overlap.py -q</automated>
  </verify>
  <done>The repo has direct regression tests for workspace construction and overlapping-run separation, while the backend and entrypoint no-cwd regressions have already been exercised in Plan 03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update existing MCP, orchestration, and report regressions to assert explicit-path execution</name>
  <files>tests/mcp/conftest.py, tests/mcp/test_tools.py, tests/test_mcp_orchestration_artifact_contract.py, tests/orchestration/test_phase3_orchestration.py, tests/test_report_credibility.py, tests/test_trustworthiness_contracts.py</files>
  <read_first>
tests/mcp/conftest.py
tests/mcp/test_tools.py
tests/test_mcp_orchestration_artifact_contract.py
tests/orchestration/test_phase3_orchestration.py
tests/test_report_credibility.py
tests/test_trustworthiness_contracts.py
edgar_project/mcp/schemas.py
edgar_project/mcp/tools.py
edgar_project/orchestration/planner.py
edgar_project/orchestration/executor.py
src/report.py
  </read_first>
  <behavior>
    - Test 1: mocked MCP path fixtures use a run-scoped layout with `processed/` and `artifacts/` subdirectories instead of a flat temp directory.
    - Test 2: orchestration assertions expect the planner label `generate_report:explicit_paths` and no normal-flow use of `use_default_artifact_paths=True`.
    - Test 3: report credibility tests assert footer and trustworthiness lookups can render workspace-scoped paths and keep any legacy-path behavior behind explicit opt-in only.
  </behavior>
  <action>Update the existing regression suites per D-02, D-03, and D-04 so they enforce explicit-path behavior instead of tolerating shared-path defaults. In `tests/mcp/conftest.py`, change `tmp_artifact_paths` to create `tmp_path / "run-123" / "processed"` and `tmp_path / "run-123" / "artifacts"` and return role paths rooted there. In `tests/mcp/test_tools.py`, add assertions that `run_workspace` is accepted by `RunPipelineInput` and `GenerateReportInput`, and keep any legacy default-path test behind explicit `use_default_artifact_paths=True`. In `tests/test_mcp_orchestration_artifact_contract.py` and `tests/orchestration/test_phase3_orchestration.py`, assert the normal planner/executor path uses `generate_report:explicit_paths` and merges workspace-scoped artifact paths. In `tests/test_report_credibility.py` and `tests/test_trustworthiness_contracts.py`, update footer/trustworthiness expectations so they accept workspace-scoped `processed/` and `artifacts/` roots, treat `manual_validation_csv` as an explicit shared input path, and only treat shared `data/artifacts` lookups as the explicit legacy branch.</action>
  <acceptance_criteria>
    - `tests/mcp/conftest.py` contains both `"processed"` and `"artifacts"` subdirectory creation under one run-scoped root.
    - `tests/orchestration/test_phase3_orchestration.py` contains the exact string `generate_report:explicit_paths`.
    - `tests/mcp/test_tools.py` contains `run_workspace`.
    - `tests/test_report_credibility.py` contains an assertion for workspace-scoped artifact paths or a workspace-aware footer helper.
    - `tests/test_trustworthiness_contracts.py` no longer relies on zero-argument `phase1_paths()` in the normal branch.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/mcp/test_tools.py tests/test_mcp_orchestration_artifact_contract.py tests/orchestration/test_phase3_orchestration.py tests/test_report_credibility.py tests/test_trustworthiness_contracts.py -q</automated>
  </verify>
  <done>Existing regression suites now fail if orchestration or report generation drifts back to shared-path defaults, loses workspace-scoped artifact provenance, or treats `manual_validation_csv` like a copied workspace artifact.</done>
</task>

</tasks>

<verification>
Phase 1 verification is complete when the focused overlap/workspace tests and the existing MCP/orchestration/report suites prove three things: overlapping runs do not collide, run/artifact metadata records the workspace that produced outputs, and normal execution never requires shared-path fallback or cwd mutation.
</verification>

<success_criteria>
`tests/test_run_isolation_overlap.py`, `tests/test_run_isolation_workspace.py`, and the updated MCP/orchestration/report suites all pass and directly assert run-scoped paths plus no implicit default-path behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-run-isolation/01-run-isolation-04-SUMMARY.md`
</output>
