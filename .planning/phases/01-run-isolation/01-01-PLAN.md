---
phase: 01-run-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - backend/config/settings.py
  - edgar_project/run_workspace.py
  - edgar_project/orchestration/execution_contract.py
  - edgar_project/orchestration/schemas.py
  - src/pipeline_runner.py
  - tests/test_run_isolation_workspace.py
  - tests/test_run_isolation_overlap.py
  - tests/test_run_isolation_execution_service.py
autonomous: true
requirements:
  - EXEC-01
  - EXEC-02
  - EXEC-03
must_haves:
  truths:
    - "A caller that knows only `workspace_root` and `run_scoped_id` can deterministically locate one processed directory and one artifact directory for that run without cwd discovery per D-01 and D-05."
    - "Downstream runtime code can receive one serialized workspace payload and resolve normal Phase 1 paths from it instead of silently drifting back to repo-global defaults per D-01, D-04, and D-05."
    - "The shared `manual_validation_csv` remains a stable explicit input reference rather than a copied per-run artifact per D-04."
    - "Shared `data/processed` and `data/artifacts` stay available only behind explicit legacy opt-in per D-03."
  artifacts:
    - path: "edgar_project/run_workspace.py"
      provides: "Shared RunWorkspace contract and deterministic path registry"
      contains: "class RunWorkspace"
    - path: "backend/config/settings.py"
      provides: "EDGAR_BACKEND_RUN_WORKSPACE_ROOT setting"
      contains: "run_workspace_root"
    - path: "tests/test_run_isolation_workspace.py"
      provides: "Contract-level workspace tests"
      contains: "test_build_run_workspace"
    - path: "tests/test_run_isolation_overlap.py"
      provides: "Wave 0 overlap regression seed"
      contains: "test_overlapping_runs_keep_distinct_artifact_paths"
    - path: "tests/test_run_isolation_execution_service.py"
      provides: "Wave 0 backend provenance regression seed"
      contains: "test_execute_analysis_run_uses_explicit_workspace_paths"
  key_links:
    - from: "backend/config/settings.py"
      to: "edgar_project/run_workspace.py"
      via: "run_workspace_root setting passed to build_run_workspace"
      pattern: "run_workspace_root"
    - from: "edgar_project/orchestration/execution_contract.py"
      to: "edgar_project/run_workspace.py"
      via: "ExecutionRequest.context['run_workspace'] payload shape matches the shared workspace contract"
      pattern: "run_workspace"
---

<objective>
Define the run-scoped workspace and artifact-path contract that every later Phase 1 change will build on.

Purpose: Establish one durable workspace layout and one serialized handoff shape before execution code starts mutating behavior.
Output: Shared workspace module, backend setting, orchestration payload shape, workspace-aware Phase 1 path registry, and contract tests.
</objective>

<execution_context>
@/Users/padraigobrien/.codex/get-shit-done/workflows/execute-plan.md
@/Users/padraigobrien/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-run-isolation/01-CONTEXT.md
@.planning/phases/01-run-isolation/01-RESEARCH.md
@config.py
@backend/config/settings.py
@edgar_project/orchestration/execution_contract.py
@edgar_project/orchestration/schemas.py
@src/pipeline_runner.py

<interfaces>
From `backend/config/settings.py`:
```python
artifact_storage_root: Path = Field(
    default=_REPO_ROOT / "data" / "artifact_storage",
)
```

From `edgar_project/orchestration/execution_contract.py`:
```python
context: JsonDict = Field(
    default_factory=dict,
    description="Optional JSON scratch for future coordinator ↔ executor data (unused today).",
)
```

From `src/pipeline_runner.py`:
```python
def phase1_paths() -> dict[str, Path]:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the shared RunWorkspace contract and config surface</name>
  <files>config.py, backend/config/settings.py, edgar_project/run_workspace.py</files>
  <read_first>
config.py
backend/config/settings.py
edgar_project/run_workspace.py (new file; confirm it does not exist yet)
.planning/phases/01-run-isolation/01-CONTEXT.md
.planning/phases/01-run-isolation/01-RESEARCH.md
docs/local-stack.md
data/README.md
  </read_first>
  <behavior>
    - Test 1: `build_run_workspace(workspace_root=/tmp/workspaces, run_scoped_id="run-123", manual_validation_csv=/repo/validation/manual_validation.csv)` yields `/tmp/workspaces/run-123/processed` and `/tmp/workspaces/run-123/artifacts`.
    - Test 2: `use_legacy_shared_paths=True` maps processed and artifact outputs back to `config.DATA_PROCESSED` and `config.DATA_ARTIFACTS`, while keeping `manual_validation_csv` pointed at the explicit shared validation file.
  </behavior>
  <action>Create `edgar_project/run_workspace.py` per D-01, D-03, D-04, D-05, and D-06 with a frozen `RunWorkspace` dataclass whose exact fields are `run_scoped_id`, `root`, `processed_dir`, `artifacts_dir`, `manual_validation_csv`, and `use_legacy_shared_paths`. Add `ensure_directories()` plus `phase1_paths()` that returns the exact existing Phase 1 output role keys and basenames: `panel.csv`, `features.csv`, `anomalies.csv`, `report.md`, `data_quality_summary.csv`, `exclusions_summary.csv`, `peer_signals.csv`, `trend_break_signals.csv`, `unified_findings.csv`, `deterioration_focus.csv`, `findings_summary_by_company.csv`, `findings_summary_by_metric.csv`, `findings_summary_by_period.csv`, `metric_coverage_summary.csv`, `metric_coverage_by_company.csv`, `metric_coverage_by_period.csv`, `metric_caveats_extraction.csv`, and `metric_caveats_panel.csv`. Keep `manual_validation_csv` as a separate explicit input path on the contract; do not model it as a copied file under `<workspace_root>/<run_scoped_id>`. Add `build_run_workspace(*, workspace_root: Path, run_scoped_id: str, manual_validation_csv: Path, use_legacy_shared_paths: bool = False, legacy_processed_dir: Path | None = None, legacy_artifacts_dir: Path | None = None) -> RunWorkspace` so the normal layout is exactly `<workspace_root>/<run_scoped_id>/processed` and `<workspace_root>/<run_scoped_id>/artifacts`, while legacy mode uses `config.DATA_PROCESSED` and `config.DATA_ARTIFACTS` only when explicitly requested. In `config.py`, add `DATA_RUNS = PROJECT_ROOT / "data" / "runs"` and include it in the eager mkdir loop; keep `DATA_PROCESSED` and `DATA_ARTIFACTS` but relabel them in comments as legacy/dev-only Phase 1 roots per D-02 and D-03. In `backend/config/settings.py`, add `run_workspace_root: Path = Field(default=_REPO_ROOT / "data" / "runs", description="Filesystem root for durable per-run processed/artifacts workspaces")` so backend and worker processes share one configured workspace root.</action>
  <acceptance_criteria>
    - `edgar_project/run_workspace.py` contains `class RunWorkspace`.
    - `edgar_project/run_workspace.py` contains `def build_run_workspace(`.
    - `edgar_project/run_workspace.py` contains both `processed_dir / "panel.csv"` and `artifacts_dir / "report.md"`.
    - `config.py` contains the exact string `DATA_RUNS = PROJECT_ROOT / "data" / "runs"`.
    - `backend/config/settings.py` contains the exact string `run_workspace_root: Path = Field(`.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/test_run_isolation_workspace.py -q</automated>
  </verify>
  <done>The repository has one reusable run workspace builder, one backend workspace-root setting, and an explicit legacy-mode branch instead of implicit shared-path behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose the workspace payload through orchestration and the Phase 1 path registry</name>
  <files>edgar_project/orchestration/execution_contract.py, edgar_project/orchestration/schemas.py, src/pipeline_runner.py, tests/test_run_isolation_workspace.py</files>
  <read_first>
edgar_project/orchestration/execution_contract.py
edgar_project/orchestration/schemas.py
src/pipeline_runner.py
edgar_project/run_workspace.py
tests/test_trustworthiness_contracts.py
tests/test_metric_coverage.py
tests/test_mcp_orchestration_artifact_contract.py
  </read_first>
  <behavior>
    - Test 1: a serialized orchestration handoff includes `context["run_workspace"]` with the exact keys `run_scoped_id`, `root`, `processed_dir`, `artifacts_dir`, `manual_validation_csv`, and `use_legacy_shared_paths`.
    - Test 2: `phase1_paths(workspace)` returns run-scoped `panel`, `features`, `anomalies`, and `report` paths, while `workspace.manual_validation_csv` remains the explicit shared validation input path instead of a workspace-copied file.
  </behavior>
  <action>Implement per D-04, D-05, and D-06 by adding a JSON-serializable `RunWorkspacePayload` model to `edgar_project/orchestration/schemas.py` with the exact string fields `run_scoped_id`, `root`, `processed_dir`, `artifacts_dir`, `manual_validation_csv`, and boolean `use_legacy_shared_paths`. In `edgar_project/orchestration/execution_contract.py`, document and type `ExecutionRequest.context["run_workspace"]` as the executor handoff slot for that payload. In `src/pipeline_runner.py`, replace the current zero-argument `phase1_paths()` seam with a workspace-aware registry so downstream code can call `phase1_paths(workspace)` and receive the same output role keys currently returned, but rooted under the supplied workspace. Keep a compatibility branch only when `use_legacy_shared_paths=True`; normal calls must not silently resolve to `config.DATA_PROCESSED` or `config.DATA_ARTIFACTS`. Keep `manual_validation_csv` explicit on the workspace payload and in tests/examples; do not add a workspace-rooted `manual_validation` artifact path. Add `tests/test_run_isolation_workspace.py` coverage for workspace payload serialization, for `phase1_paths(workspace)` returning `/processed/panel.csv`, `/processed/features.csv`, `/artifacts/anomalies.csv`, and `/artifacts/report.md`, and for `workspace.manual_validation_csv` preserving the shared validation input path.</action>
  <acceptance_criteria>
    - `edgar_project/orchestration/schemas.py` contains `class RunWorkspacePayload`.
    - `edgar_project/orchestration/execution_contract.py` contains the exact string `run_workspace`.
    - `src/pipeline_runner.py` contains `def phase1_paths(workspace`.
    - `tests/test_run_isolation_workspace.py` contains `test_build_run_workspace`, `test_phase1_paths_are_run_scoped`, `test_manual_validation_csv_remains_explicit_input`, and `test_run_workspace_payload_round_trip`.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/test_run_isolation_workspace.py tests/test_trustworthiness_contracts.py -q</automated>
  </verify>
  <done>The execution handoff can carry run workspace metadata and the central Phase 1 path registry can resolve run-scoped paths without relying on cwd or repo-global defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Seed Wave 0 isolation regression modules before runtime adoption</name>
  <files>tests/test_run_isolation_overlap.py, tests/test_run_isolation_execution_service.py, tests/test_run_isolation_workspace.py</files>
  <read_first>
tests/test_run_isolation_workspace.py
tests/test_backend_foundation.py
tests/test_mcp_orchestration_artifact_contract.py
backend/services/edgar_pipeline_execution_service.py
backend/services/artifact_service.py
edgar_project/run_workspace.py
  </read_first>
  <behavior>
    - Test 1: `tests/test_run_isolation_overlap.py` exists with a collected `test_overlapping_runs_keep_distinct_artifact_paths` placeholder that names the shared role keys Phase 1 must keep distinct across `run-a` and `run-b`.
    - Test 2: `tests/test_run_isolation_execution_service.py` exists with a collected `test_execute_analysis_run_uses_explicit_workspace_paths` placeholder that asserts future backend execution must persist `run_workspace` provenance and avoid cwd mutation.
    - Test 3: `tests/test_run_isolation_workspace.py` includes notes or TODO-shaped assertions that link the shared contract helpers to the later overlap and backend-provenance regressions.
  </behavior>
  <action>Create the Wave 0 regression seeds required by `01-VALIDATION.md` so Phase 1 has early test placeholders before runtime work lands. Add `tests/test_run_isolation_overlap.py` with a collected placeholder `test_overlapping_runs_keep_distinct_artifact_paths` that builds two `RunWorkspace` values such as `run-a` and `run-b`, names the shared role keys `panel`, `features`, `anomalies`, `report`, and `unified_findings`, and records that later plans must prove those paths never collide. Add `tests/test_run_isolation_execution_service.py` with a collected placeholder `test_execute_analysis_run_uses_explicit_workspace_paths` that names the expected `run_workspace` metadata payload, the persisted `analysis_run_id` anchor, and the rule that `chdir_repo_root()` must not be used in the normal backend execution path. In `tests/test_run_isolation_workspace.py`, add cross-references or TODO markers that point to those future regressions so the Wave 0 contract files exist and collect cleanly even before the runtime implementation in Plans 02 through 04.</action>
  <acceptance_criteria>
    - `tests/test_run_isolation_overlap.py` contains `def test_overlapping_runs_keep_distinct_artifact_paths`.
    - `tests/test_run_isolation_overlap.py` contains each exact string `panel`, `features`, `anomalies`, `report`, and `unified_findings`.
    - `tests/test_run_isolation_execution_service.py` contains `def test_execute_analysis_run_uses_explicit_workspace_paths`.
    - `tests/test_run_isolation_execution_service.py` contains both exact strings `run_workspace` and `chdir_repo_root`.
    - `tests/test_run_isolation_workspace.py` contains cross-reference text for both `test_overlapping_runs_keep_distinct_artifact_paths` and `test_execute_analysis_run_uses_explicit_workspace_paths`.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/test_run_isolation_overlap.py tests/test_run_isolation_execution_service.py --collect-only -q</automated>
  </verify>
  <done>Phase 1 has Wave 0 regression modules in place before runtime adoption begins, so later plans can expand them instead of introducing the coverage from scratch at the end.</done>
</task>

</tasks>

<verification>
The contract layer is complete when a downstream executor can read one workspace payload, build one deterministic set of Phase 1 paths from it, keep legacy shared paths behind an explicit boolean only, and inherit Wave 0 regression seeds for overlap and backend provenance.
</verification>

<success_criteria>
`edgar_project/run_workspace.py` exists, `EDGAR_BACKEND_RUN_WORKSPACE_ROOT` has a code path in settings, `ExecutionRequest.context["run_workspace"]` is defined, and the Wave 0 files `tests/test_run_isolation_workspace.py`, `tests/test_run_isolation_overlap.py`, and `tests/test_run_isolation_execution_service.py` are all planned in the earliest wave.
</success_criteria>

<output>
After completion, create `.planning/phases/01-run-isolation/01-run-isolation-01-SUMMARY.md`
</output>
