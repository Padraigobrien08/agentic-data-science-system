---
phase: 01-run-isolation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - backend/services/edgar_pipeline_execution_service.py
  - backend/services/artifact_service.py
  - main.py
  - edgar_project/cli.py
  - edgar_project/console_digest.py
  - docker-compose.yml
  - docs/local-stack.md
  - data/README.md
  - tests/test_run_isolation_execution_service.py
  - tests/test_run_isolation_entrypoints.py
autonomous: true
requirements:
  - EXEC-02
  - EXEC-03
must_haves:
  truths:
    - "Persisted backend runs anchor their workspace contract to the persisted run identity per D-05 and D-06."
    - "Non-DB entry points generate compatible run-scoped workspaces by default instead of writing to shared `data/processed` and `data/artifacts` per D-02, D-03, and D-06."
    - "API and worker deployments share one configured run workspace root and document that shared volume explicitly per D-01 and D-05."
  artifacts:
    - path: "backend/services/edgar_pipeline_execution_service.py"
      provides: "Persisted-run workspace creation, context handoff, and no-cwd backend execution"
      contains: "run_workspace"
    - path: "tests/test_run_isolation_execution_service.py"
      provides: "Backend provenance and no-cwd regression coverage in the same wave as the runtime change"
      contains: "test_execute_analysis_run_uses_explicit_workspace_paths"
    - path: "main.py"
      provides: "Non-DB direct entry point that writes into a generated run-scoped workspace"
      contains: "write_all_phase1_artifacts"
    - path: "edgar_project/cli.py"
      provides: "CLI execution without repo-root chdir and with run-scoped path guidance"
      contains: "main("
    - path: "tests/test_run_isolation_entrypoints.py"
      provides: "CLI and main.py run-workspace regression coverage in the same wave as the entrypoint change"
      contains: "test_cli_and_main_use_run_scoped_workspaces_without_chdir"
    - path: "docker-compose.yml"
      provides: "Shared run workspace volume and `EDGAR_BACKEND_RUN_WORKSPACE_ROOT` wiring"
      contains: "run_workspaces"
  key_links:
    - from: "backend/services/edgar_pipeline_execution_service.py"
      to: "backend/services/artifact_service.py"
      via: "artifact metadata merges `run_workspace` with `source_path`"
      pattern: "run_workspace"
    - from: "main.py"
      to: "edgar_project/run_workspace.py"
      via: "non-DB entry point builds a compatible generated workspace id"
      pattern: "build_run_workspace"
    - from: "docker-compose.yml"
      to: "docs/local-stack.md"
      via: "shared `/var/lib/edgar/run_workspaces` volume and env var documentation"
      pattern: "run_workspaces"
---

<objective>
Migrate the backend and non-DB entry points to the shared run-workspace contract, then align deployment/docs with that runtime.

Purpose: Close the D-06 gap so persisted runs, CLI/demo flows, and the direct `main.py` entry point all use compatible run-scoped identities without cwd mutation or shared default output directories.
Output: Backend provenance persistence, no-cwd backend/CLI execution, run-scoped `main.py` behavior, and Compose/docs updates for a shared workspace root.
</objective>

<execution_context>
@/Users/padraigobrien/.codex/get-shit-done/workflows/execute-plan.md
@/Users/padraigobrien/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-run-isolation/01-CONTEXT.md
@.planning/phases/01-run-isolation/01-RESEARCH.md
@edgar_project/run_workspace.py
@backend/services/edgar_pipeline_execution_service.py
@backend/services/artifact_service.py
@main.py
@edgar_project/cli.py
@docker-compose.yml
@docs/local-stack.md

<interfaces>
From `backend/services/artifact_service.py`:
```python
def ingest_pipeline_file(
    self,
    source_path: Path | str,
    *,
    role_key: str,
    analysis_run_id: UUID | None = None,
    meta_json: dict | list | None = None,
) -> Artifact:
```

From `backend/services/edgar_pipeline_execution_service.py`:
```python
ensure_repo_root_on_syspath()
chdir_repo_root()
...
for role_key, path_str in out.artifact_paths.items():
```

From `main.py`:
```python
write_all_phase1_artifacts(...)
print(f"\nWrote: {config.DATA_PROCESSED / 'panel.csv'}")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Anchor persisted backend runs to `analysis_run_id` workspaces and persist provenance without cwd mutation</name>
  <files>backend/services/edgar_pipeline_execution_service.py, backend/services/artifact_service.py, tests/test_run_isolation_execution_service.py</files>
  <read_first>
backend/services/edgar_pipeline_execution_service.py
backend/services/artifact_service.py
backend/config/settings.py
backend/models/analysis_run.py
edgar_project/run_workspace.py
edgar_project/orchestration/execution_contract.py
tests/test_run_isolation_execution_service.py
  </read_first>
  <behavior>
    - Test 1: a persisted backend run builds `run_workspace` from `settings.run_workspace_root` plus `str(analysis_run_id)` and stores the same payload in `ExecutionRequest.context["run_workspace"]` and `analysis_run.meta_json["run_workspace"]`.
    - Test 2: ingested artifacts preserve `source_path` and merge the same `run_workspace` payload into artifact `meta_json`.
    - Test 3: `tests/test_run_isolation_execution_service.py::test_execute_analysis_run_uses_explicit_workspace_paths` proves the normal backend execution path completes without calling `chdir_repo_root()` and persists the workspace provenance payload.
  </behavior>
  <action>Implement per D-05 and D-06 by making persisted backend runs create the workspace payload from `settings.run_workspace_root` and the persisted `analysis_run_id`, then hand that payload to orchestration and persistence. In `backend/services/edgar_pipeline_execution_service.py`, build `RunWorkspace`, serialize it into `ExecutionRequest.context["run_workspace"]`, persist the same payload into `analysis_run.meta_json["run_workspace"]`, and pass `meta_json={"orchestration_run_id": out.run_id, "source": "pipeline", "run_workspace": {...}}` into `ArtifactService.ingest_pipeline_file`. Remove the normal `chdir_repo_root()` dependency from this service; keep `ensure_repo_root_on_syspath()` only if imports still require it. In `backend/services/artifact_service.py`, preserve `source_path` and merge `run_workspace` into artifact metadata so provenance survives after ingest. Expand `tests/test_run_isolation_execution_service.py` from its Wave 0 seed into a real regression that mocks orchestration output, asserts persisted run metadata plus artifact `meta_json["run_workspace"]`, and proves the backend service does not invoke cwd-mutation helpers. Keep `manual_validation_csv` inside the workspace payload as the explicit shared input path; do not turn it into a copied workspace artifact.</action>
  <acceptance_criteria>
    - `backend/services/edgar_pipeline_execution_service.py` contains `run_workspace_root` and `run_workspace`.
    - `backend/services/edgar_pipeline_execution_service.py` no longer imports or calls `chdir_repo_root`.
    - `backend/services/artifact_service.py` merges `run_workspace` alongside `source_path`.
    - `tests/test_run_isolation_execution_service.py` contains `test_execute_analysis_run_uses_explicit_workspace_paths`.
    - The plan text and implementation keep `manual_validation_csv` as the shared explicit input path.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/test_backend_foundation.py tests/test_run_isolation_execution_service.py -q && rg -n "run_workspace|run_workspace_root" backend/services/edgar_pipeline_execution_service.py backend/services/artifact_service.py && ! rg -n "chdir_repo_root\\(" backend/services/edgar_pipeline_execution_service.py</automated>
  </verify>
  <done>Persisted runs, ingested artifacts, and backend execution all share one `analysis_run_id`-anchored workspace payload and the backend no longer depends on cwd mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate `main.py`, CLI/demo guidance, and Compose/docs to the run-workspace default</name>
  <files>main.py, edgar_project/cli.py, edgar_project/console_digest.py, docker-compose.yml, docs/local-stack.md, data/README.md, tests/test_run_isolation_entrypoints.py</files>
  <read_first>
main.py
edgar_project/cli.py
edgar_project/console_digest.py
docker-compose.yml
docs/local-stack.md
data/README.md
edgar_project/run_workspace.py
tests/test_run_isolation_entrypoints.py
  </read_first>
  <behavior>
    - Test 1: `main.py` builds a compatible generated run-scoped workspace and writes under `data/runs/<run_scoped_id>/processed` and `/artifacts` by default, with shared `data/processed` / `data/artifacts` only behind explicit legacy opt-in.
    - Test 2: `tests/test_run_isolation_entrypoints.py::test_cli_and_main_use_run_scoped_workspaces_without_chdir` proves CLI main no longer calls `_chdir_repo_root()` or `chdir_repo_root()` before dispatch, and user-facing guidance refers to run-scoped workspace paths instead of shared default directories.
    - Test 3: Compose sets `EDGAR_BACKEND_RUN_WORKSPACE_ROOT=/var/lib/edgar/run_workspaces`, mounts a named `run_workspaces` volume into both `api` and `worker`, and `docs/local-stack.md` documents that shared path explicitly.
  </behavior>
  <action>Implement per D-02, D-03, D-05, and D-06 by removing shared-path and cwd assumptions from the non-DB entry points and deployment docs. In `main.py`, build a compatible generated workspace id via `build_run_workspace()` and print the actual resolved run-scoped paths instead of `config.DATA_PROCESSED` / `config.DATA_ARTIFACTS`; keep any shared-path behavior behind an explicit dev/legacy opt-in only. In `edgar_project/cli.py`, remove `_chdir_repo_root()` and replace the hardcoded guidance that live runs write to `data/processed/` and `data/artifacts/` with `data/runs/<run_scoped_id>/processed` and `data/runs/<run_scoped_id>/artifacts`; keep legacy wording only behind explicit opt-in. In `edgar_project/console_digest.py`, update the live run formatter so the stdout digest no longer advertises shared `data/processed/` and `data/artifacts/` roots as the normal output location and instead shows the actual run-scoped workspace or `data/runs/<run_scoped_id>/...` guidance. In `docker-compose.yml`, add the exact env var `EDGAR_BACKEND_RUN_WORKSPACE_ROOT: /var/lib/edgar/run_workspaces`, create a named `run_workspaces` volume, and mount it into both `api` and `worker`. In `docs/local-stack.md` and `data/README.md`, document that shared workspace root/volume and state that API and worker must both see `/var/lib/edgar/run_workspaces` for durable run workspaces, while `data/processed` and `data/artifacts` are legacy shared paths rather than the normal live-run default. Expand `tests/test_run_isolation_entrypoints.py` from its Wave 0 seed into a real regression that exercises the CLI/main guidance and confirms no repo-root chdir helper is used in the normal branch.</action>
  <acceptance_criteria>
    - `main.py` contains `data/runs` guidance or prints run-scoped resolved paths instead of shared `config.DATA_PROCESSED` / `config.DATA_ARTIFACTS`.
    - `edgar_project/cli.py` contains `data/runs/<run_scoped_id>` guidance and no longer contains `_chdir_repo_root` or `chdir_repo_root`.
    - `edgar_project/console_digest.py` no longer advertises shared `data/processed/` and `data/artifacts/` roots as the normal live-run output location.
    - `docker-compose.yml` contains the exact string `EDGAR_BACKEND_RUN_WORKSPACE_ROOT: /var/lib/edgar/run_workspaces`.
    - `docker-compose.yml` contains a `run_workspaces:` named volume mounted into both `api` and `worker`.
    - `docs/local-stack.md` contains `/var/lib/edgar/run_workspaces`.
    - `data/README.md` contains `data/runs/` guidance and labels `processed` / `artifacts` as legacy shared paths when Phase 1 isolation is enabled.
    - `tests/test_run_isolation_entrypoints.py` contains `test_cli_and_main_use_run_scoped_workspaces_without_chdir`.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/test_run_isolation_entrypoints.py -q && rg -n "EDGAR_BACKEND_RUN_WORKSPACE_ROOT: /var/lib/edgar/run_workspaces|run_workspaces:" docker-compose.yml && rg -n "data/runs/<run_scoped_id>|/var/lib/edgar/run_workspaces" docs/local-stack.md edgar_project/cli.py edgar_project/console_digest.py main.py data/README.md && ! rg -n "_chdir_repo_root|chdir_repo_root" edgar_project/cli.py</automated>
  </verify>
  <done>`main.py`, CLI/demo guidance, and the documented Compose stack all default to the run-workspace contract, with shared legacy output paths available only behind explicit opt-in.</done>
</task>

</tasks>

<verification>
This plan is complete when persisted backend runs, non-DB entry points, and the documented API/worker deployment all describe and use the same run-scoped workspace contract without cwd mutation.
</verification>

<success_criteria>
Persisted runs store workspace provenance rooted in `analysis_run_id`, `tests/test_run_isolation_execution_service.py` and `tests/test_run_isolation_entrypoints.py` both pass in the same wave as the runtime change, CLI and `main.py` no longer default to shared output directories or repo-root `chdir`, and Compose/docs explicitly wire `/var/lib/edgar/run_workspaces`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-run-isolation/01-run-isolation-03-SUMMARY.md`
</output>
