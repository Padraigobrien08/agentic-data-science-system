---
phase: 01-run-isolation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/pipeline_runner.py
  - src/report.py
  - src/data_quality.py
  - src/exclusions.py
  - src/manual_validation.py
  - src/metric_caveats.py
  - edgar_project/mcp/adapters.py
  - edgar_project/mcp/schemas.py
  - edgar_project/mcp/tools.py
  - edgar_project/mcp/server.py
  - edgar_project/orchestration/planner.py
  - edgar_project/orchestration/executor.py
autonomous: true
requirements:
  - EXEC-01
  - EXEC-02
must_haves:
  truths:
    - "A normal Phase 1 run can write processed files and artifacts into its own workspace without colliding with any sibling run per D-01 and D-02."
    - "The report step and its trustworthiness readers can finish from explicit workspace paths or injected workspace metadata without depending on repo-global defaults per D-04."
    - "MCP and orchestration can carry the same explicit workspace and artifact references through the normal report step, with shared default paths available only through deliberate legacy opt-in per D-04 and D-05."
  artifacts:
    - path: "src/pipeline_runner.py"
      provides: "Workspace-aware Phase 1 writers"
      contains: "write_all_phase1_artifacts"
    - path: "src/report.py"
      provides: "Workspace-aware report and credibility readers"
      contains: "generate_report"
    - path: "edgar_project/orchestration/executor.py"
      provides: "Runtime path injection from execution context"
      contains: "_dispatch_mcp"
    - path: "edgar_project/orchestration/planner.py"
      provides: "Normal report-step plan that expects explicit paths"
      contains: "generate_report:explicit_paths"
  key_links:
    - from: "src/pipeline_runner.py"
      to: "src/report.py"
      via: "workspace-derived artifact paths reused by report readers and footer rendering"
      pattern: "workspace"
    - from: "edgar_project/orchestration/executor.py"
      to: "edgar_project/mcp/tools.py"
      via: "GenerateReportInput and RunPipelineInput built from accumulated artifact_paths + context['run_workspace']"
      pattern: "run_workspace"
    - from: "edgar_project/orchestration/planner.py"
      to: "edgar_project/orchestration/executor.py"
      via: "planner leaves explicit-path placeholders that executor fills from accumulated artifact paths"
      pattern: "generate_report:explicit_paths"
---

<objective>
Refactor the deterministic writers/readers plus MCP/orchestration wiring so normal execution stays on explicit run-scoped paths end-to-end.

Purpose: Make the Phase 1 execution path genuinely isolated in live runs instead of defining a contract that the normal writer and report flow still bypass.
Output: Workspace-aware writers/readers and MCP/orchestration path threading that keep normal execution on explicit run-scoped paths.
</objective>

<execution_context>
@/Users/padraigobrien/.codex/get-shit-done/workflows/execute-plan.md
@/Users/padraigobrien/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-run-isolation/01-CONTEXT.md
@.planning/phases/01-run-isolation/01-RESEARCH.md
@edgar_project/run_workspace.py
@src/pipeline_runner.py
@src/report.py
@src/manual_validation.py
@src/metric_caveats.py
@edgar_project/mcp/schemas.py
@edgar_project/mcp/tools.py
@edgar_project/orchestration/planner.py
@edgar_project/orchestration/executor.py

<interfaces>
From `edgar_project/mcp/schemas.py`:
```python
class GenerateReportInput(BaseModel):
    anomalies_csv_path: str | None = None
    features_csv_path: str | None = None
    use_default_artifact_paths: bool = Field(default=False)
```

From `edgar_project/orchestration/planner.py`:
```python
PlannedStep(
    tool_name=TOOL_GENERATE_REPORT,
    tool_input={
        "anomalies_csv_path": None,
        "features_csv_path": None,
        "use_default_artifact_paths": True,
    },
)
```

From `src/manual_validation.py`:
```python
VALIDATION_CSV_PATH = config.PROJECT_ROOT / "validation" / "manual_validation.csv"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor Phase 1 writers and report/manual-validation readers to consume explicit workspace paths</name>
  <files>src/pipeline_runner.py, src/report.py, src/data_quality.py, src/exclusions.py, src/manual_validation.py, src/metric_caveats.py</files>
  <read_first>
src/pipeline_runner.py
src/report.py
src/data_quality.py
src/exclusions.py
src/manual_validation.py
src/metric_caveats.py
edgar_project/run_workspace.py
tests/test_run_isolation_workspace.py
tests/test_metric_coverage.py
tests/test_metric_caveats.py
tests/test_deterioration_focus.py
tests/test_report_credibility.py
tests/test_trustworthiness_contracts.py
  </read_first>
  <behavior>
    - Test 1: writing Phase 1 outputs with one `RunWorkspace(run_scoped_id="run-a")` writes `panel.csv` and `features.csv` under `.../run-a/processed/` and writes `anomalies.csv` and `report.md` under `.../run-a/artifacts/`.
    - Test 2: report credibility helpers load trustworthiness CSVs from the supplied workspace paths and keep `workspace.manual_validation_csv` as the shared validation input reference; they only fall back to legacy shared paths when `use_legacy_shared_paths=True`.
    - Test 3: a normal-mode generated `report.md` never renders hardcoded `data/artifacts/...` provenance strings from report helper modules.
  </behavior>
  <action>Implement per D-02, D-03, and D-04 by making every normal Phase 1 writer use the workspace contract from Plan 01. In `src/pipeline_runner.py`, change `write_panel_csv`, `write_features_csv`, `write_anomalies_csv`, `write_report_md`, `write_data_quality_csv`, `write_exclusions_csv`, `write_peer_signals_csv`, `write_trend_breaks_csv`, `write_unified_findings_csv`, `write_deterioration_focus_csv`, `write_findings_summary_by_*_csv`, `write_metric_coverage_artifacts`, and `write_all_phase1_artifacts` so they write under `workspace.processed_dir` or `workspace.artifacts_dir` instead of `config.DATA_PROCESSED` and `config.DATA_ARTIFACTS`. In `src/metric_caveats.py`, route caveat CSV writes through the same workspace artifact directory. In `src/report.py`, stop `_try_read_artifact_csv` and `_artifact_paths_footer` from hardcoding `config.DATA_ARTIFACTS`; accept an explicit workspace/path map and render the exact workspace-derived basenames from that map. In `src/data_quality.py` and `src/exclusions.py`, remove hardcoded report-footer strings that point to `data/artifacts/data_quality_summary.csv` and `data/artifacts/exclusions_summary.csv` in normal mode; make those helpers consume the explicit workspace/path map and only emit shared `data/artifacts/...` text when `use_legacy_shared_paths=True`. In `src/manual_validation.py`, stop defaulting `load_panel()` and CLI help text to `data/processed/panel.csv` in normal flow; make the explicit panel path the normal branch, keep `VALIDATION_CSV_PATH` / `manual_validation_csv` as the shared explicit input path, and keep the legacy shared-path branch only when `use_legacy_shared_paths=True`.</action>
  <acceptance_criteria>
    - `src/pipeline_runner.py` contains `workspace.processed_dir` and `workspace.artifacts_dir`.
    - `src/report.py` no longer contains the exact line `p = config.DATA_ARTIFACTS / basename` in the normal path-loading helper.
    - `src/data_quality.py` and `src/exclusions.py` no longer emit repo-global `data/artifacts/...` footer text in the normal report branch.
    - `src/manual_validation.py` no longer hardcodes `config.DATA_PROCESSED / "panel.csv"` as the only normal path branch and still keeps `manual_validation_csv` explicit.
    - `src/metric_caveats.py` contains `workspace.artifacts_dir`.
    - `tests/test_report_credibility.py` contains an assertion that normal-mode report output does not include repo-global `data/artifacts/` strings while legacy mode can still render them behind opt-in.
    - `tests/test_metric_caveats.py` and `tests/test_deterioration_focus.py` are either updated or explicitly exercised by the verification command for the workspace-aware writer contract.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/test_run_isolation_workspace.py tests/test_report_credibility.py tests/test_metric_coverage.py tests/test_metric_caveats.py tests/test_deterioration_focus.py tests/test_trustworthiness_contracts.py -q</automated>
  </verify>
  <done>Phase 1 file writers and report/manual-validation readers resolve paths from an explicit workspace contract, with legacy shared paths gated behind an explicit boolean and `manual_validation_csv` preserved as a shared input path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread workspace data through MCP and orchestration without default-path fallbacks</name>
  <files>edgar_project/mcp/adapters.py, edgar_project/mcp/schemas.py, edgar_project/mcp/tools.py, edgar_project/mcp/server.py, edgar_project/orchestration/planner.py, edgar_project/orchestration/executor.py</files>
  <read_first>
edgar_project/mcp/adapters.py
edgar_project/mcp/schemas.py
edgar_project/mcp/tools.py
edgar_project/mcp/server.py
edgar_project/orchestration/planner.py
edgar_project/orchestration/executor.py
edgar_project/run_workspace.py
tests/mcp/test_tools.py
tests/test_mcp_orchestration_artifact_contract.py
tests/orchestration/test_phase3_orchestration.py
  </read_first>
  <behavior>
    - Test 1: the orchestration executor fills `panel_csv_path`, `features_csv_path`, `anomalies_csv_path`, and `run_workspace` from accumulated runtime state before it constructs MCP input models.
    - Test 2: the planner no longer emits `use_default_artifact_paths=True` in the normal report step.
  </behavior>
  <action>Implement per D-04 and D-05 by threading explicit workspace data through the MCP/orchestration boundary. In `edgar_project/mcp/schemas.py`, add a `run_workspace` field typed as the Plan 01 payload on `GenerateReportInput` and `RunPipelineInput`; keep `use_default_artifact_paths` only as an explicit legacy branch and keep its default `False`. Keep `manual_validation_csv` as an explicit field inside that payload rather than inventing a copied workspace `manual_validation` artifact path. In `edgar_project/mcp/server.py`, expose the new optional workspace fields in the `generate_report` and `run_pipeline` tool wrappers. In `edgar_project/mcp/adapters.py` and `edgar_project/mcp/tools.py`, build or reuse `RunWorkspace` objects so normal `generate_report_tool` and `run_pipeline_tool` read and write through explicit workspace paths instead of repo-global defaults. In `edgar_project/orchestration/planner.py`, change the final granular report step label from `generate_report:default_paths` to `generate_report:explicit_paths` and stop hardcoding `use_default_artifact_paths=True`. In `edgar_project/orchestration/executor.py`, change `_dispatch_mcp` so it can see both `ExecutionRequest.context["run_workspace"]` and the accumulated `artifact_paths` map; before constructing `ComputeFeaturesInput`, `DetectAnomaliesInput`, and `GenerateReportInput`, inject the exact upstream file paths from `panel_csv`, `features_csv`, and `anomalies_csv` when the planner left those fields empty. Normal executor dispatch must never require repo-global `data/processed` or `data/artifacts` to finish the report step.</action>
  <acceptance_criteria>
    - `edgar_project/orchestration/planner.py` contains `generate_report:explicit_paths`.
    - `edgar_project/orchestration/planner.py` does not contain `use_default_artifact_paths": True`.
    - `edgar_project/mcp/schemas.py` contains `run_workspace`.
    - `edgar_project/orchestration/executor.py` contains both `artifact_paths` and `run_workspace` in the MCP dispatch path.
    - `edgar_project/mcp/server.py` exposes workspace parameters on both `generate_report` and `run_pipeline`.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/mcp/test_tools.py tests/test_mcp_orchestration_artifact_contract.py tests/orchestration/test_phase3_orchestration.py -q</automated>
  </verify>
  <done>MCP and orchestration pass explicit run-scoped paths end-to-end, the normal report step no longer depends on default shared artifact paths, and the shared `manual_validation_csv` stays an explicit input path.</done>
</task>

</tasks>

<verification>
This plan is complete when writers, readers, MCP, and orchestration can carry one run workspace payload plus explicit artifact paths through the normal report flow without falling back to shared `data/processed` or `data/artifacts`.
</verification>

<success_criteria>
Normal planner/executor flow no longer uses `use_default_artifact_paths=True`, artifact paths are run-scoped, and `manual_validation_csv` remains an explicit shared input path instead of a workspace-copied file.
</success_criteria>

<output>
After completion, create `.planning/phases/01-run-isolation/01-run-isolation-02-SUMMARY.md`
</output>
