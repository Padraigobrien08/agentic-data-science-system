/**
 * The product's name, in one place.
 *
 * The landing page called it "auditable-loop" and the app shell called it "EDGAR Analysis",
 * so clicking the hero CTA took a reader from one product to what looked like another — and
 * the browser tab said the second name on both. EDGAR is an adapter, not the architecture;
 * naming the whole system after it undersells the part that took the work.
 */
export const PRODUCT_NAME = "auditable-loop";

export const PRODUCT_TAGLINE =
  "An agent that investigates a dataset adaptively, and can show you every step of its " +
  "reasoning back to the number it came from.";
