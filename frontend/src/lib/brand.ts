/**
 * The name, in one place.
 *
 * Deliberately a descriptor rather than a coined brand: this is a portfolio project, and the
 * name's job is to let someone scanning a list of repositories understand what it is without
 * clicking. "auditable-agent-loop" says the three things that matter — it is an agent, it
 * runs a loop, and the loop is auditable.
 *
 * It has been three names at once before: the landing page said "auditable-loop", the app
 * shell said "EDGAR Analysis", and the repository said "agentic-data-science-system", so a
 * reader crossing between them saw what looked like three products. EDGAR is an adapter, not
 * the architecture; naming the whole system after it undersells the part that took the work.
 */
export const PRODUCT_NAME = "auditable-agent-loop";

export const PRODUCT_TAGLINE =
  "An investigation loop that proposes competing explanations, tests each against " +
  "deterministic analysis, and can show you every claim it makes back to the number it " +
  "came from.";
